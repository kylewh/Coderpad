import Immutable from 'immutable'

const initialState = Immutable.fromJS({})

const musicTime = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default musicTime
